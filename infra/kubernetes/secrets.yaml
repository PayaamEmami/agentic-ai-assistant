# WARNING: This is a template. Do not commit real secrets.
# Use sealed-secrets, external-secrets-operator, or AWS Secrets Manager in production.
apiVersion: v1
kind: Secret
metadata:
  name: aaa-secrets
  namespace: aaa
type: Opaque
stringData:
  DATABASE_URL: "postgresql://user:password@host:5432/aaa"
  REDIS_URL: "redis://host:6379"
  OPENAI_API_KEY: "sk-placeholder"
